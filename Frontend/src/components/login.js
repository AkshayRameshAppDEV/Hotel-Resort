import React, { Component } from "react";

// This is the Login page when the user clicks the Register button in navbar or register icon in the right of the middle section in landing page
class Login extends Component{
    render(){
        return(
            <div><h1>Login Page</h1></div>
        )
    }
}

export default Login;